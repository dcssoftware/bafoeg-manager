linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

# linters-settings:

output:
  formats:
    - format: colored-line-number
      path: stdout  

run:
  timeout: 5m
  tests: false